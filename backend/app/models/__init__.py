from app.models.models import (
    User, UserRole, Driver, Bus, BusStatus, Route, Stop, RouteStop, 
    Trip, TripStatus, BusLocation, ETAPrediction, Alert, AlertSeverity, 
    Favorite, FavoriteType
)
